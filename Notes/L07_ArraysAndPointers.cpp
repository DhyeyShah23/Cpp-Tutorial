#include <iostream>

using namespace std;

int main() {
    
// DYNAMIC ARRAYS

    /*
    Arrays are allocated on the heap using the new operator.
    We can also use new(std::nothrow) to avoid exception in the program.
    */
}

